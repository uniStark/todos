services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:3000"
    volumes:
      - todos-data:/app/data
    environment:
      - NODE_ENV=production
      - DATA_DIR=/app/data
    restart: always

volumes:
  todos-data:
