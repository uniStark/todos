services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./todos.json:/app/todos.json
    environment:
      - NODE_ENV=production
    restart: always

volumes:
  todos-data:
