services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3002:3000"
    volumes:
      - todos-data:/app/data
    environment:
      - NODE_ENV=production
      - DATA_DIR=/app/data
      # 设置访问密码（可自定义，默认为 stark123）
      - AUTH_PASSWORD=${AUTH_PASSWORD:-stark123}
      # AI 大模型配置 (SiliconFlow API)
      - AI_API_KEY=${AI_API_KEY:?AI_API_KEY required, set in .env or shell}
      - AI_BASE_URL=${AI_BASE_URL:-https://api.siliconflow.cn/v1}
      - AI_MODEL_GLM4=${AI_MODEL_GLM4:-zai-org/GLM-4.6}
      - AI_MODEL_DEEPSEEK=${AI_MODEL_DEEPSEEK:-deepseek-ai/DeepSeek-V3.1-Terminus}
      - AI_DEFAULT_MODEL=${AI_DEFAULT_MODEL:-deepseek_v3.1}
      - AI_TEMPERATURE=${AI_TEMPERATURE:-0.7}
      - AI_MAX_TOKENS=${AI_MAX_TOKENS:-2000}
      - AI_TIMEOUT=${AI_TIMEOUT:-600}
    restart: always

volumes:
  todos-data:
